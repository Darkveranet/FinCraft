/**
 * AUTO-GENERATED by tools/api-automation/generate-e2e.mjs — DO NOT EDIT BY HAND.
 * Regenerate: `npm run api:e2e-gen`. Hand edits are overwritten on the next run.
 * Required-field negative-validation: 13 matched, zero-path-param write operations whose contract declares a non-empty required[] list. Sends an empty body and expects rejection. NOTE: Fineract's spec marks 'required' explicitly on only a minority of operations — this list is NOT exhaustive of what Fineract actually requires at runtime, only of what the contract documents.
 */
import { test } from '@playwright/test';
import { fineractExpectRejected } from './helpers-shared.mjs';

test.describe('generated: required-field validation', () => {
  test("createCollateral_1 rejects empty body (missing: basePrice, currency, locale, name, pctToBase, quality, unitType)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/collateral-management", {});
  });
  test("createDatatable rejects empty body (missing: apptableName, columns, datatableName)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/datatables", {});
  });
  test("createLoanOriginator rejects empty body (missing: name)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/loan-originators", {});
  });
  test("createPaymentType rejects empty body (missing: isSystemDefined, name)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/paymenttypes", {});
  });
  test("createQuote rejects empty body (missing: accountId, amount, amountType, quoteCode, transactionCode, transactionRole)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/interoperation/quotes", {});
  });
  test("createStaff rejects empty body (missing: firstname, lastname, officeId)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/staff", {});
  });
  test("createTransactionRequest rejects empty body (missing: accountId, amount, transactionCode, transactionRole)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/interoperation/requests", {});
  });
  test("performTransfer rejects empty body (missing: accountId, amount, transactionCode, transactionRole, transferCode)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/interoperation/transfers", {});
  });
  test("switchCache rejects empty body (missing: cacheType)", async ({ request }) => {
    await fineractExpectRejected(request, "PUT", "/caches", {});
  });
  test("updateBusinessDate rejects empty body (missing: date, dateFormat, locale, type)", async ({ request }) => {
    await fineractExpectRejected(request, "POST", "/businessdate", {});
  });
  test("updateCurrencies rejects empty body (missing: currencies)", async ({ request }) => {
    await fineractExpectRejected(request, "PUT", "/currencies", {});
  });
  test("updateExternalEventConfigurations rejects empty body (missing: externalEventConfigurations)", async ({ request }) => {
    await fineractExpectRejected(request, "PUT", "/externalevents/configuration", {});
  });
  test("updateInstanceMode rejects empty body (missing: batchManagerEnabled, batchWorkerEnabled, readEnabled, writeEnabled)", async ({ request }) => {
    await fineractExpectRejected(request, "PUT", "/instance-mode", {});
  });
});
